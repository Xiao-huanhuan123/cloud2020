server:
  port: 8001

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.1.149:3306/cloud?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimeZone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    #初始化链接数
    initialSize: 5
    #最小的空闲连接数
    minIdle: 5
    #最大活动连接数
    maxActive: 20
    #从池中取连接的最大等待时间，单位ms
    maxWait: 60000
    #每XXms运行一次空闲连接回收器
    timeBetweenEvictionRunsMillis: 60000
    #池中的连接空闲XX毫秒后被回收
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT1FROMDUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    filters: stat,wall,log4j
    logSlowSql: true


mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.whlearn.springcloud.entities

eureka:
  client:
    fetch-registry: true #表示将自己注册进EurekaServer
    fetchRegistry: true #从EurekaServer抓取已有的注册信息，单节点不影响，集群必须设置为true才能ribbon使用负载均衡
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka #集群
#      defaultZone: http://eureka7001.com:7001/eureka #单机
  instance:
    instance-id: payment8001
    prefer-ip-address: true #访问路径可显示IP地址
#    lease-renewal-interval-in-seconds: 1 #向eureka server发送心跳的时间间隔，单位是秒
#    lease-expiration-duration-in-seconds: 2 #eureka server收到最后一次心跳后等待的时间上限，超时将剔除服务